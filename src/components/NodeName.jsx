import React from "react";

const NodeName = ({ name }) => {
  return <div style={{ background: "grey", padding: "2px 5px", borderRadius: "3px", color: "white", fontSize: "10px", marginTop: "5px" }}>{name}</div>;
};

export default NodeName;
